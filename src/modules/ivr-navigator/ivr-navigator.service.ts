import { Injectable, Logger } from '@nestjs/common';

@Injectable()
export class IvrNavigatorService {
  private readonly logger = new Logger(IvrNavigatorService.name);

  constructor() {}

  // IVR navigation methods will be implemented here
}